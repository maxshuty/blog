{
  "host": "${process.env.BACKEND_HOST}",
  "port": "${process.env.BACKEND_PORT}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
