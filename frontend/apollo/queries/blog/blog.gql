query Blog($id: ID!) {
  blog(id: $id) {
      id
      title
      subtitle
      authors {
        firstName
        lastName
        relationship
      }
      date
      body
      attachments {
          url
      }
    }
}
